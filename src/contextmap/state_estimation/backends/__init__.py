"""Concrete State Estimation backends.

Backends are internal to the capability: other capabilities import
``contextmap.state_estimation``, never this package. Only the runtime
composition root constructs them.
"""
